#Link to a guide:
# https://github.com/rom1504/sshd_android

#Need:
# termux
#	apt install openssh autossh
# termux:boot
#	run once to enable
#	startup scripts to be located in ~/.termux/boot/

#Must copy public key from remote host to phone's ~/.ssh/known_hosts


#example script
#~/.termux/boot/start_autossh.sh
#!/data/data/com.termux/files/usr/bin/sh
termux-wake-lock
sshd
autossh -M 0 -f -R 199i42:localhost:8022 -N  -o "ExitOnForwardFailure yes" -o "ServerAliveInterval 30" -o "ServerAliveCountMax 1" user@myserver.com


