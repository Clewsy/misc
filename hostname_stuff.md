# Modify in /etc/hostname
```shell
$ vim /etc/hostname
```

# Modify in /etc/hosts
```shell
$ vim /etc/hosts
```

# Update without reboot
```shell
$ /etc/init.d/hostname.sh start
```

# Verify
```shell
$ hostname
```

