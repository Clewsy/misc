# List dconf settings for an app
E.g. for guake:
```shell
$ dconf list /apps/guake/general/
```

# Change/set a setting (key):
E.g. Change guake transperancy:
```shell
$ dconf write /apps/guake/style/background/transperancy 50
```
