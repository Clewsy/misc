##To encrypt a file
gpg -c file.txt
#Enter passphrase (x2) - note, encrypted file (file.txt.gpg) is created but original file is retained.
rm file.txt
# -c	:	Encrypt with a symmetric cipher using a passphrase.  Default cipher is AES128

##Decrypt a file
gpg -d file.txt.gpg
#This outputs file contents to stdout.
gpg -d -o file.txt file.txt.gpg
#This creates a new, decrypted file.


#Disable cacheing of keys by gpg-agent
vim ~/.gnupg/gpg-agent.conf
#add:
default-cache-ttl 0
max-cache-ttl 0
#Write, exit and run:
gpgconf --reload gpg-agent
